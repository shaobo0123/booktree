{
  "name": "tree-bookmarks",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently \"node scripts/run-bun.cjs --cwd server --watch src/main.ts\" \"npm run dev -w client\"",
    "dev:client": "npm run dev -w client",
    "dev:server": "node scripts/run-bun.cjs --cwd server --watch src/main.ts",
    "build": "node scripts/run-bun.cjs build src/main.ts --outdir=dist --target=bun --cwd server && npm run build -w client"
  },
  "overrides": {
    "csstype": "3.1.3"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
